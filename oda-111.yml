tosca_definitions_version: tosca_simple_yaml_1_2
#This version is not yet valid as slide 16 has not yet introduced values for the mandatory type field

description: Template for physical router within a tosca node.network
topology_template:
  node_templates:

    my_port:
      type: # Not yet defined
      capabilities:
          # linkable is inherited  
      requirements:
        - binding: router_software

    router_software:
      type: # Not yet defined
      capabilities:
          # bindable is inherited
      requirements:
        - host: processor_card
    


