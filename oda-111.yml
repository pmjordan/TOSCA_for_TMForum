tosca_definitions_version: tosca_simple_yaml_1_0

dsl_definitions:
# Create a yaml alias for parts of the TMForum Information Model which are frequently reused 
  PhysicalResourceSpecAttributes: &PhysicalResourceSpecAttributes
    vendorName:
      type: string
    partNumber:
      type: string

node_types:
  tosca.nodes.Root.PhysicalResource.Hardware:
    derived_from: tosca.nodes.Root

  tosca.nodes.Root.PhysicalResource.Hardware.PhysicalPort:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware
    capabilities:
      link:
        type: tosca.capabilities.network.Linkable

  tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware

  tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware

  tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer

  tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment.Card:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment

  tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.EquipmentHolder:
    derived_from: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer

  tosca.nodes.Compute.NetworkDevice:
    derived_from: tosca.nodes.Compute
    # provides level of indirection between SoftwareComponent and Processor-card

  tosca.nodes.Root.CompoundResource:
    derived_from: tosca.nodes.Root
    properties:
      property1:
        type: string
      property2:
          type: integer
    attributes:
      attribute1:
        type: float
      attribute2:
        type: boolean

description: Template for physical router within a tosca node.network
topology_template:
  node_templates:

    my_port:
      type: tosca.nodes.Root.PhysicalResource.Hardware.PhysicalPort
      # capabilities:
          # linkable is inherited
      requirements:
        - dependency: router_software
        - dependency: port_card

    router_software:
      type: tosca.nodes.SoftwareComponent
      #capabilities: timos
      requirements:
        - host: device_compute
            
    device_compute:
      type: tosca.nodes.Compute.NetworkDevice
      requirements:
        - dependency: processor_card

    processor_card:
      type: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment.Card
      attributes: *PhysicalResourceSpecAttributes
      requirements:
        - dependency: slot

    port_card:
      type: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment.Card
      attributes: *PhysicalResourceSpecAttributes
      requirements:
        - dependency: slot

    slot:
      type: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.EquipmentHolder
      requirements:
        - dependency: chassis

    chassis:
      type: tosca.nodes.Root.PhysicalResource.Hardware.ManagedHardware.PhysicalContainer.Equipment
      attributes: *PhysicalResourceSpecAttributes

    router:
      type: tosca.nodes.Root.CompoundResource
      properties:
        property1: a
        property2: 1
      attributes:
        attribute1: 2.0
        attribute2: true
      requirements:
        - dependency: router_software
        - dependency: chassis


